#include <fstream> // ofstream
#include <iostream> // ios_base

#include <gtest/gtest.h>

#include <cudmd/types.h>

using std::ios_base;
using std::ofstream;

TEST(CudmdTest, ToDo) {
    ASSERT_EQ(1, 1);
}
